#include<bits/stdc++.h>
using namespace std;

// Calculate area for right angle triangle .


int main()
{
    float h,b,a;  

    // h=height , b=base , a=area 

    cout << "Enter the Value of height and base respectively" ;

    cin >> h >> b ;


    a = ( h * b ) / 2.0 ;
   
    cout << "Area of triangle is = " << a ;

  return 0;
  
}